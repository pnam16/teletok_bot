import {join} from "path";

/**
 * Root directory for any state or output files your bot needs.
 * This is not used by the template itself but is provided for convenience.
 */
export const DATA_DIR = join(process.cwd(), "dist");
